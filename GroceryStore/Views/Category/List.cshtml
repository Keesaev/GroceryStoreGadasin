@model IEnumerable<Category>
<h2>
    Категории:
</h2>
@foreach (var c in Model)
{
    <div class="card card-outline-primary m-1 p-1">
        <h3>
            @Html.ActionLink(@c.Name, "List", "Product", new { categoryName = c.Name })
        </h3>
        @c.Description
    </div>
}