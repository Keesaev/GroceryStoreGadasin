@model IEnumerable<string>
<h4>
    Категории:
</h4>
@foreach (var c in Model)
{
    <div class="card card-outline-primary m-1 p-1">
        <h5>
            @Html.ActionLink(c, "List", "Product", new { category = c})
        </h5>
    </div>
}