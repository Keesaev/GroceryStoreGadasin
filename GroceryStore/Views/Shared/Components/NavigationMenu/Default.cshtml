@model IEnumerable<string>
@foreach (string category in Model)
{
     @Html.ActionLink(@category, "List", "Product", new { category = @category }, new { @class="btn btn-block btn-secondary btn-light"})
}